# Быстрый старт: Развертывание в public_html с MySQL

## ✅ Что уже сделано

Проект готов к развертыванию на хостинге:
- ✅ База данных изменена с PostgreSQL на **MySQL**
- ✅ Все пути относительные - работают в любой папке
- ✅ Создан .htaccess для Apache
- ✅ Добавлен .env.example с примером для MySQL
- ✅ Код не требует изменений при переносе из `vargos-backend` в `public_html`

## 🚀 Инструкция для хостинга

### 1️⃣ Скопируйте файлы на хостинг

Загрузите все файлы из папки `vargos-backend` в папку `public_html` на вашем хостинге:

**Важно:** Именно содержимое папки `vargos-backend`, а не саму папку!

```
Было на GitHub:
endProject/
└── vargos-backend/
    ├── src/
    ├── public/
    ├── prisma/
    └── ...

Должно быть на хостинге:
public_html/
├── src/
├── public/
├── prisma/
├── package.json
└── ...
```

### 2️⃣ Создайте MySQL базу данных

Через панель управления хостингом (например, cPanel):
1. Перейдите в "MySQL Databases"
2. Создайте новую базу данных (например: `user_vargos`)
3. Создайте пользователя (например: `user_admin`)
4. Добавьте пользователя к базе данных с полными правами
5. Запомните: имя БД, имя пользователя, пароль

### 3️⃣ Настройте .env файл

Создайте файл `.env` в `public_html/`:

```env
DATABASE_URL="mysql://user_admin:your_password@localhost:3306/user_vargos"
JWT_SECRET=измените_на_свой_секретный_ключ_минимум_32_символа
PORT=4000
NODE_ENV=production
```

**Замените:**
- `user_admin` → ваш пользователь MySQL
- `your_password` → ваш пароль MySQL
- `user_vargos` → имя вашей базы данных
- `JWT_SECRET` → любая случайная строка (минимум 32 символа)

### 4️⃣ Установите зависимости

Подключитесь по SSH к хостингу:

```bash
cd public_html
npm install
```

### 5️⃣ Настройте базу данных

```bash
npx prisma generate
npx prisma migrate deploy
```

### 6️⃣ Запустите приложение

```bash
npm start
```

Или для постоянной работы (рекомендуется):

```bash
npm install -g pm2
pm2 start src/index.js --name vargos
pm2 save
pm2 startup
```

### 7️⃣ Проверьте работу

Откройте в браузере:
- `http://ваш-домен.com/` - главная страница
- `http://ваш-домен.com/health` - должен вернуть `{"status":"ok"}`

## 📋 Чек-лист

- [ ] Файлы скопированы в `public_html`
- [ ] MySQL база данных создана
- [ ] Файл `.env` создан и настроен
- [ ] Выполнено `npm install`
- [ ] Выполнено `npx prisma generate`
- [ ] Выполнено `npx prisma migrate deploy`
- [ ] Приложение запущено
- [ ] `/health` endpoint возвращает OK
- [ ] Можно открыть главную страницу

## ⚠️ Частые проблемы

### "Cannot find module"
**Решение:** Выполните `npm install` и `npx prisma generate`

### "Environment variable not found: DATABASE_URL"
**Решение:** Проверьте наличие файла `.env` в `public_html/`

### "Access denied for user"
**Решение:** Проверьте правильность DATABASE_URL в `.env`

### "Port 4000 already in use"
**Решение:** Измените PORT в `.env` или остановите процесс на этом порту

### Приложение останавливается после закрытия SSH
**Решение:** Используйте PM2 для запуска приложения

## 📚 Подробная документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство по развертыванию
- [MYSQL_MIGRATION.md](MYSQL_MIGRATION.md) - Миграция с PostgreSQL
- [HOSTING_EXAMPLES.md](HOSTING_EXAMPLES.md) - Примеры для разных хостингов
- [PUBLIC_HTML_SETUP.md](PUBLIC_HTML_SETUP.md) - Детальная инструкция

## 💡 Важные заметки

1. **Папка vargos-backend → public_html**: Это просто переименование папки. Код не требует изменений.

2. **MySQL вместо PostgreSQL**: Все настроено. Только нужно создать MySQL базу на хостинге.

3. **Безопасность**: Файл `.htaccess` уже настроен и защищает:
   - `.env` файл
   - Папки `src/`, `node_modules/`, `prisma/`
   - Конфигурационные файлы

4. **Статические файлы**: Все HTML, CSS, JS из папки `public/` доступны напрямую через браузер.

5. **API**: Все API endpoints работают через тот же домен (настроено в `.htaccess`).

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи: `pm2 logs` (если используете PM2)
2. Проверьте подключение к БД: `mysql -u user -p -h localhost database`
3. Откройте issue в GitHub репозитории
