# Новые возможности / New Features

## 🚀 Оптимизация для хостинга (Hosting Optimization)

### Серверные улучшения:
- ✅ Добавлено сжатие HTTP ответов (compression middleware)
- ✅ Добавлены заголовки безопасности (helmet middleware)
- ✅ Кэширование статических файлов (1 день)
- ✅ Увеличен лимит размера JSON до 10MB для больших смет
- ✅ Добавлены индексы базы данных для ускорения запросов

### Оптимизация базы данных:
- Индексы на `Quote(userId, createdAt)` для быстрой загрузки истории
- Индексы на `Quote(userId, name)` для поиска смет по имени
- Индексы на `Category(userId)` для быстрой загрузки категорий

---

## 📚 История версий смет (Version History)

### Автоматическое сохранение версий:
- При каждом сохранении сметы автоматически создается версия
- Все изменения отслеживаются и сохраняются
- Возможность просмотра всех версий каждой сметы

### Использование:
1. Откройте историю смет через меню профиля
2. Нажмите кнопку "Версии" на нужной смете
3. Просмотрите все сохраненные версии
4. Сравните две версии с помощью кнопки "Сравнить"

### API эндпоинты:
- `GET /quotes/:id/versions` - получить список версий
- `GET /quotes/:id/compare?v1=X&v2=Y` - сравнить две версии

---

## 🔌 Оффлайн-режим (Offline Mode)

### Service Worker:
- Автоматическое кэширование всех статических ресурсов
- Работа приложения без интернета на стройплощадках
- Автоматическая синхронизация при восстановлении соединения

### Индикаторы статуса:
- 🟢 Зелёный баннер при восстановлении соединения
- 🔴 Красный баннер при потере соединения
- Автоматическое уведомление о доступности новой версии

### Кэширование:
- **Network First** для API запросов (свежие данные приоритетны)
- **Cache First** для статических файлов (быстрая загрузка)
- Автоматическое обновление кэша при доступности сети

---

## 💾 Автосохранение каждые 5 секунд (Auto-save)

### Функции автосохранения:
- ✅ Сохранение каждые 5 секунд (вместо 2 секунд)
- ✅ Визуальный индикатор "✓ Автосохранено" в правом верхнем углу
- ✅ Автоматическое восстановление при перезагрузке страницы
- ✅ Сохранение временной метки последнего сохранения

### Восстановление данных:
1. При перезагрузке страницы проверяется наличие несохраненных данных
2. Если данные найдены (не старше 24 часов), показывается диалог восстановления
3. Пользователь может выбрать: восстановить или начать заново
4. После восстановления данные автоматически очищаются

### Что сохраняется:
- Название проекта
- Все комнаты и работы
- Количество, цены, категории
- Заметки
- Настройки конфигурации (VAT, режим калькулятора и т.д.)

---

## 🛠️ Технические детали

### Новые файлы:
- `public/service-worker.js` - Service Worker для оффлайн режима
- `public/js/offline-manager.js` - Управление Service Worker и статусом соединения
- `prisma/migrations/...add_quote_versions/` - Миграция для версий
- `prisma/migrations/...add_indexes/` - Миграция для индексов

### Обновленные файлы:
- `src/index.js` - Добавлено сжатие и безопасность
- `src/controllers/quoteController.js` - Логика версионирования
- `src/routes/quoteRoutes.js` - Новые эндпоинты для версий
- `public/js/auto-save.js` - Улучшенное автосохранение
- `public/js/main.js` - Интеграция всех новых функций
- `public/js/quotes-api.js` - API для работы с версиями

### База данных:
```sql
-- Новая таблица QuoteVersion
CREATE TABLE "QuoteVersion" (
    "id" SERIAL PRIMARY KEY,
    "quoteId" INTEGER NOT NULL,
    "version" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "total" DOUBLE PRECISION NOT NULL,
    "notes" TEXT,
    "config" JSONB,
    "data" JSONB NOT NULL,
    "createdAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP
);

-- Новые поля в Quote
ALTER TABLE "Quote" 
    ADD COLUMN "updatedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    ADD COLUMN "version" INTEGER DEFAULT 1,
    ADD COLUMN "config" JSONB;
```

---

## 📋 Использование (Usage)

### Для разработки:
```bash
cd vargos-backend
npm install
npm run dev
```

### Для продакшена:
```bash
cd vargos-backend
npm install
npm start
```

### Переменные окружения (.env):
```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
PORT=4000
JWT_SECRET=your-secret-key
```

---

## 🔄 Миграции базы данных

После клонирования проекта выполните:
```bash
npx prisma migrate deploy
```

Или для разработки:
```bash
npx prisma migrate dev
```

---

## 🎯 Основные преимущества

1. **Надёжность**: Данные не теряются при перезагрузке страницы
2. **Производительность**: Быстрая работа благодаря кэшированию и сжатию
3. **Автономность**: Работа без интернета на объектах
4. **История**: Полный контроль версий и возможность отката
5. **Безопасность**: Защита заголовков и оптимизация под продакшен

---

## 📝 Примечания

- Автосохранение работает только в localStorage браузера
- Версии сохраняются на сервере при каждом сохранении через кнопку "Сохранить"
- Service Worker активируется автоматически при первом посещении
- Для обновления Service Worker требуется перезагрузка страницы

---

## 🐛 Troubleshooting

### Service Worker не регистрируется:
- Убедитесь, что сайт работает через HTTPS (или localhost)
- Проверьте консоль браузера на ошибки
- Очистите кэш браузера

### Автосохранение не работает:
- Проверьте, что localStorage доступен
- Убедитесь, что не включен режим "инкогнито"
- Проверьте размер доступного хранилища

### Версии не сохраняются:
- Убедитесь, что пользователь авторизован
- Проверьте подключение к базе данных
- Убедитесь, что миграции применены
