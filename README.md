# Vargos - Проект управления сметами

Веб-приложение для создания и управления строительными сметами.

## 🚀 Технологии

- **Backend:** Node.js, Express.js
- **Database:** MySQL (было PostgreSQL)
- **ORM:** Prisma
- **Frontend:** HTML, CSS, JavaScript

## 📦 Установка

### Для разработки

```bash
cd vargos-backend
npm install
```

### Настройка базы данных

1. Создайте MySQL базу данных
2. Скопируйте `.env.example` в `.env`
3. Обновите `DATABASE_URL` в `.env`:

```env
DATABASE_URL="mysql://username:password@localhost:3306/database_name"
JWT_SECRET=your_secret_key
PORT=4000
```

4. Примените миграции:

```bash
npx prisma generate
npx prisma migrate deploy
# или для разработки:
npx prisma migrate dev
```

### Запуск

```bash
# Режим разработки
npm run dev

# Продакшн
npm start
```

## 📁 Структура проекта

```
vargos-backend/
├── prisma/
│   ├── migrations/          # Миграции базы данных
│   └── schema.prisma        # Схема БД (MySQL)
├── public/                  # Статические файлы
│   ├── css/
│   ├── js/
│   └── index.html
├── src/                     # Серверный код
│   ├── controllers/         # Контроллеры API
│   ├── middlewares/         # Middleware (auth и др.)
│   ├── routes/             # Маршруты API
│   ├── db/                 # Конфигурация БД
│   └── index.js            # Точка входа
├── .env.example            # Пример конфигурации
├── .htaccess              # Конфигурация Apache
└── package.json
```

## 🌐 Развертывание на хостинге

### Краткая инструкция

1. Скопируйте все файлы из папки `vargos-backend` в `public_html` на хостинге
2. Создайте MySQL базу данных через панель хостинга
3. Создайте `.env` файл с настройками БД
4. Установите зависимости: `npm install`
5. Примените миграции: `npx prisma migrate deploy`
6. Запустите: `npm start` или используйте PM2

### Подробные инструкции

- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Полное руководство по развертыванию
- **[PUBLIC_HTML_SETUP.md](PUBLIC_HTML_SETUP.md)** - Инструкция по переносу в public_html
- **[MYSQL_MIGRATION.md](MYSQL_MIGRATION.md)** - Миграция с PostgreSQL на MySQL

## 🔑 API Endpoints

### Авторизация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /auth/me` - Текущий пользователь

### Категории
- `GET /categories` - Список категорий
- `POST /categories` - Создать категорию
- `PUT /categories/:id` - Обновить категорию
- `DELETE /categories/:id` - Удалить категорию

### Работы (Jobs)
- `GET /jobs` - Список работ
- `POST /jobs` - Создать работу
- `PUT /jobs/:id` - Обновить работу
- `DELETE /jobs/:id` - Удалить работу

### Котировки (Quotes)
- `GET /quotes` - Список котировок
- `GET /quotes/:id` - Получить котировку
- `POST /quotes` - Создать котировку
- `PUT /quotes/:id` - Обновить котировку
- `DELETE /quotes/:id` - Удалить котировку

### Здоровье сервиса
- `GET /health` - Проверка работоспособности

## 🔐 Безопасность

- JWT аутентификация
- Bcrypt для хеширования паролей
- `.htaccess` защита для чувствительных файлов
- CORS настройки
- Заголовки безопасности

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

Nodemon автоматически перезапустит сервер при изменении файлов.

### Работа с базой данных

```bash
# Создать новую миграцию
npx prisma migrate dev --name migration_name

# Применить миграции
npx prisma migrate deploy

# Открыть Prisma Studio (GUI для БД)
npx prisma studio

# Сгенерировать Prisma Client
npx prisma generate
```

## 📝 Изменения для хостинга

### Изменена база данных: PostgreSQL → MySQL

- Обновлен `prisma/schema.prisma` - провайдер теперь `mysql`
- Обновлен `prisma/migrations/migration_lock.toml`
- Добавлен `.env.example` с примером подключения к MySQL

### Добавлена поддержка public_html

- Создан `.htaccess` для Apache конфигурации
- Защита чувствительных файлов (.env, node_modules, src)
- Настройка проксирования API запросов
- Кеширование статических файлов

### Документация

- Подробные инструкции по развертыванию
- Руководство по миграции БД
- Инструкции по настройке для public_html

## 📄 Лицензия

Не определена (проект в разработке)

## 👤 Автор

NooAS
